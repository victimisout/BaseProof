`package.json`

This file defines the metadata for your project and lists the dependencies needed to run the client-side (frontend) of the application.

Here’s an example of what `package.json` might look like:

```json
{
  "name": "baseproof",
  "version": "1.0.0",
  "description": "A dApp for proving the existence and integrity of documents on the Base blockchain.",
  "main": "src/index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.0",
    "web3": "^1.7.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.0"
  },
  "keywords": [
    "base",
    "blockchain",
    "proof",
    "document",
    "verification"
  ],
  "author": "victimisout",
  "license": "MIT"
}
